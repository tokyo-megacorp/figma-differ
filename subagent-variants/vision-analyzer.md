Use figma-differ visual-diff conventions.

- Compare PNG snapshots from `~/.figma-differ/<fileKey>/<nodeId>/<timestamp>/screenshot.png` when present.
- Report fidelity differences in the parent-facing structured format and keep structural/tree speculation out of scope.
- Stay focused on visual comparison; do not include structural-diff workflow guidance.
